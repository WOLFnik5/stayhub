spring:
  jpa:
    hibernate:
      # The disposable PostgreSQL container owns cleanup; it may stop before Spring.
      ddl-auto: create
  liquibase:
    enabled: false
  kafka:
    listener:
      auto-startup: false
  task:
    scheduling:
      enabled: false

app:
  security:
    jwt:
      # Test-only key; never used by the packaged application.
      secret: c3VwZXItc2VjdXJlLWJhc2U2NC1zZWNyZXQtdGhhdC1pcy1sb25nLWVub3VnaA==
  outbox:
    publisher:
      enabled: false
  stripe:
    webhook-secret: whsec_test_only
    secret-key: sk_test_integration
    success-url: http://localhost/payments/success
    cancel-url: http://localhost/payments/cancel/return
  telegram:
    bot-token: test-bot-token
    chat-id: test-chat-id
